# Cloudflare Pages configuration
# This file tells Cloudflare where to find and how to build the app

[build]
command = "cd 'AiTuki react' && npm install && npm run build"
publish = "AiTuki react/build"

[build.environment]
NODE_VERSION = "18"
CI = "false"

